/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['daisyui.com', 'https://tailus.io', 'i.ibb.co', 'lh3.googleusercontent.com', 'avatars.githubusercontent.com', 'graph.facebook.com'],
    },
};

export default nextConfig;
